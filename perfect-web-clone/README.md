# Method B · Perfect-Web-Clone

Vite + React residual assemble of the Meta Newsroom Muse article.

Pipeline: `pwc extract → plan → assemble` of **8 sections**, plus captured `public/theme.css`.

This is a study clone. Not an official Meta page. Pink study banner: `:43212`.

```bash
npm install
npm run dev   # http://0.0.0.0:43212
```

## Honesty

- Terminal state: **`failed_with_residuals`**
- Full-page SSIM **0.582** (threshold 0.97)
- Weight gate also failed (gzipped code over budget)
- Human review still prefers this arm over Method A because assemble keeps captured classes + `theme.css`

Videos and the LISTEN audio play from remote about.fb.com URLs. Local `public/assets/videos/` binaries are **not** in this repo.

See `NOTES.md` (and `public/NOTES.md` for the in-app notes link).
