# Method A · JCodesMore

Study clone of [Introducing Muse](https://about.fb.com/news/2026/09/introducing-muse-personal-ai-agent/) using the JCodesMore `/clone-website` method: Playwright + `getComputedStyle()`, section specs under `docs/research/`, Next.js rebuild.

Not an official Meta page. Green study banner: `:43211`. **Control only** — Method B is the default going forward.

QA is side-by-side eyeball. **No pixelmatch.**

```bash
npm install
npm run dev
```

Dev server: `http://0.0.0.0:43211`
